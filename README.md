-- I started to make roblox games so no more scripts from now on
-- 26.10.2024
local function returns(c)
	if c then return "Quit to make roblox games no more scripts"
	end
end
game:GetService("Players").LocalPlayer:Kick(returns())
